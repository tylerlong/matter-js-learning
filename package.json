{
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.0.11",
    "gts": "^2.0.2",
    "typescript": "^3.9.5",
    "yarn-upgrade-all": "^0.5.2"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run check"
  }
}
